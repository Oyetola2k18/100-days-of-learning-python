#round function to round up values or round down, it take two arguments
#first one is the variable that holds the floating point number and 
#the second one means to how many digits(on default it justs rounds it uo to the nearest integer)

print(round(2/3))

#there is an operator used for division but will only return integer values
#it is also know as the floor operator
print(5//2)


#another trick
result = 4/2
result /= 2  #same as result = result / 2
print(result)